import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    fontSize: 16,
    width: "100%",
    height: 35,
    borderBottomWidth: 1,
    borderColor: "#363636",
    marginTop: "10%",
  },
});
