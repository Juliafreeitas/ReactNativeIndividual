import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  styleButton: {
    width: "100%",
    height: 45,
    borderRadius: 12,
    alignItems: "center",
    justifyContent: "center",
  },

  text: {
    color: "white",
    fontSize: 17,
    fontWeight: "bold",
  },
});
